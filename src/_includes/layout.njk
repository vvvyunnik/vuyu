<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title }} - vuyu.dev</title>
  <link rel="icon" href="/favicon.ico?v=1" type="image/x-icon" />
  <link rel="shortcut icon" href="/favicon.ico?v=1" type="image/x-icon" />
  <link rel="stylesheet" href="/css/terminal.min.css" />
  <link rel="stylesheet" href="/css/main.css" />
</head>
<body class="terminal">
  <main class="main">
    {{ content | safe }}
  </main>
  <div class="terminal-card menu">
      <nav>
        <ul>
          {% set navPages = collections.all | eleventyNavigation %}
          {% for entry in navPages %}
            {% if entry.url != page.url %}
              <li>
                <a href="{{ entry.url }}">{{ entry.title }}</a>
              </li>
            {% endif %}
          {% endfor %}

          {% if collections.articles and collections.articles.length and page.url != '/articles/' %}
            {% for article in collections.articles %}
              {% if article.url != page.url %}
                <li>
                  <a href="{{ article.url }}">
                    [{{ loop.index0 }}] {{ article.data.eleventyNavigation.title }}
                  </a>
                </li>
              {% endif %}
            {% endfor %}
          {% endif %}
        </ul>
      </nav>
  </div>
</body>
</html>
