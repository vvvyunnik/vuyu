<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title }}</title>
  <link rel="icon" href="/assets/favicon.ico" type="image/x-icon" />
  <link rel="stylesheet" href="/css/terminal.min.css" />
  <style>
    :root {
      --background-color: #1d1f21;
      --font-color: #c5c8c6;
      --primary-color: #81a2be;
      --invert-font-color: #fff;
      --secondary-color: #707880;
    }

    body.terminal {
      margin: 0;
      background-color: var(--background-color);
      color: var(--font-color);
      font-family: var(--mono-font-stack);
    }

    .layout {
      display: flex;
      min-height: 100vh;
    }

    aside.terminal-menu {
      flex: 0 0 15%;
      padding: 2rem 1rem;
      border-right: 1px solid var(--secondary-color);
    }

    main.main-content {
      flex: 0 0 60%;
      padding: 2rem;
    }

    nav ul {
      list-style: none;
      margin: 0;
      padding: 0;
    }

    nav li {
      font-size: 1.2rem;
      margin-bottom: 0.5rem;
      color: var(--primary-color);
    }

    nav li ul {
      margin-top: 0.5rem;
      margin-left: 1.5rem;
      padding: 0;
    }

    nav li ul li {
      margin-bottom: 0.3rem;
    }

    nav a {
      color: var(--primary-color);
      text-decoration: none;
    }

    nav a:hover {
      text-decoration: underline;
    }

    .secondary-color {
      color: var(--secondary-color);
    }

    aside.terminal-menu {
      flex: 0 0 25%;
      padding: 2rem 1rem;
      border-right: 1px solid var(--secondary-color);
    }

    .terminal-menu ul {
      display: flex;
      flex-direction: column !important;
      align-items: flex-start;
      padding: 0 !important;
    }

    .submenu {
      margin-left: -120px;
    }
  </style>
</head>
<body class="terminal">
  <div class="layout">
    <aside class="terminal-menu">
      <nav>
        <ul>
          <li>
            <a href="{{ '/index/' | url }}">me</a>
          </li>
          <li>
            <a href="{{ '/articles/' | url }}">weekly.sh</a>
            <ul class="submenu">
              {% if collections.articles.length %}
                {% for article in collections.articles %}
                  <li>
                    <a href="{{ article.url }}">
                      {{ article.date | date("yyyy_MM_dd") }} {{ article.data.title }}
                    </a>
                  </li>
                {% endfor %}
              {% else %}
                <li><span class="secondary-color">No articles yet!</span></li>
              {% endif %}
            </ul>
          </li>
        </ul>
      </nav>
    </aside>

    <main class="main-content">
      {{ content | safe }}
    </main>
  </div>
</body>
</html>
